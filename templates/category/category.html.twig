{% extends 'base.html.twig' %}

{% block title %}Update category{% endblock %}

{% block body %}

    <div class="container update-category">

       {{ form_start(form) }}
        {{ form_widget(form.name) }}
        <button type="submit">Valider</button>
       {{ form_end(form) }}

       <a href="{{ path('app_category_remove', {'id': id }) }}">Supprimer</a>

    </div>

{% endblock %}